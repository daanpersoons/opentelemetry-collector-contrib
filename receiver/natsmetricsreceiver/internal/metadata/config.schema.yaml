# Code generated by mdatagen. DO NOT EDIT.
$defs:
  metrics_config:
    description: MetricsConfig provides config for natsmetrics metrics.
    type: object
    properties:
      nats.account.bytes.received:
        description: "NatsAccountBytesReceivedMetricConfig provides config for the nats.account.bytes.received metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nats.account.bytes.sent:
        description: "NatsAccountBytesSentMetricConfig provides config for the nats.account.bytes.sent metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nats.account.connections:
        description: "NatsAccountConnectionsMetricConfig provides config for the nats.account.connections metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nats.account.leaf_nodes:
        description: "NatsAccountLeafNodesMetricConfig provides config for the nats.account.leaf_nodes metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nats.account.messages.received:
        description: "NatsAccountMessagesReceivedMetricConfig provides config for the nats.account.messages.received metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nats.account.messages.sent:
        description: "NatsAccountMessagesSentMetricConfig provides config for the nats.account.messages.sent metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nats.account.slow_consumers:
        description: "NatsAccountSlowConsumersMetricConfig provides config for the nats.account.slow_consumers metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nats.account.subscriptions:
        description: "NatsAccountSubscriptionsMetricConfig provides config for the nats.account.subscriptions metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nats.connection.count:
        description: "NatsConnectionCountMetricConfig provides config for the nats.connection.count metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nats.jetstream.api.calls:
        description: "NatsJetstreamAPICallsMetricConfig provides config for the nats.jetstream.api.calls metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nats.jetstream.api.errors:
        description: "NatsJetstreamAPIErrorsMetricConfig provides config for the nats.jetstream.api.errors metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nats.jetstream.bytes:
        description: "NatsJetstreamBytesMetricConfig provides config for the nats.jetstream.bytes metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nats.jetstream.consumers:
        description: "NatsJetstreamConsumersMetricConfig provides config for the nats.jetstream.consumers metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nats.jetstream.memory.reserved:
        description: "NatsJetstreamMemoryReservedMetricConfig provides config for the nats.jetstream.memory.reserved metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nats.jetstream.messages:
        description: "NatsJetstreamMessagesMetricConfig provides config for the nats.jetstream.messages metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nats.jetstream.storage.reserved:
        description: "NatsJetstreamStorageReservedMetricConfig provides config for the nats.jetstream.storage.reserved metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nats.jetstream.streams:
        description: "NatsJetstreamStreamsMetricConfig provides config for the nats.jetstream.streams metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nats.server.bytes.received:
        description: "NatsServerBytesReceivedMetricConfig provides config for the nats.server.bytes.received metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nats.server.bytes.sent:
        description: "NatsServerBytesSentMetricConfig provides config for the nats.server.bytes.sent metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nats.server.connections:
        description: "NatsServerConnectionsMetricConfig provides config for the nats.server.connections metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nats.server.connections.total:
        description: "NatsServerConnectionsTotalMetricConfig provides config for the nats.server.connections.total metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nats.server.cpu:
        description: "NatsServerCPUMetricConfig provides config for the nats.server.cpu metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nats.server.leaf_nodes:
        description: "NatsServerLeafNodesMetricConfig provides config for the nats.server.leaf_nodes metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nats.server.max_connections:
        description: "NatsServerMaxConnectionsMetricConfig provides config for the nats.server.max_connections metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nats.server.memory:
        description: "NatsServerMemoryMetricConfig provides config for the nats.server.memory metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nats.server.messages.received:
        description: "NatsServerMessagesReceivedMetricConfig provides config for the nats.server.messages.received metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nats.server.messages.sent:
        description: "NatsServerMessagesSentMetricConfig provides config for the nats.server.messages.sent metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nats.server.routes:
        description: "NatsServerRoutesMetricConfig provides config for the nats.server.routes metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nats.server.slow_consumers:
        description: "NatsServerSlowConsumersMetricConfig provides config for the nats.server.slow_consumers metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
      nats.server.subscriptions:
        description: "NatsServerSubscriptionsMetricConfig provides config for the nats.server.subscriptions metric."
        type: object
        properties:
          enabled:
            type: boolean
            default: true
  metrics_builder_config:
    description: MetricsBuilderConfig is a configuration for natsmetrics metrics builder.
    type: object
    properties:
      metrics:
        $ref: metrics_config
